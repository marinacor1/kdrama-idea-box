<h1>My Personal Idea</h1>
<li>
<p><%= @idea.title %></p>
<p><%= @idea.description %></p>
</li>
<%= link_to "Edit Idea", edit_user_idea_path(@user) %>
<p><%= link_to "Delete Idea", user_idea_path(@user, @idea), method: :delete, data: {confirm: "Really delete this idea?"} %></p>
<p><%= link_to "All Ideas", user_ideas_path(@user) %></p>
